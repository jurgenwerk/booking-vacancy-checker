name: Check Booking Availability

on:
  schedule:
    # Run every hour
    - cron: "0 * * * *"
  workflow_dispatch: # Allow manual triggering

jobs:
  check-availability:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run build

      - name: Run availability check
        run: npm start

      - name: Check for changes and create issue
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            const checkAvailability = require('./check-availability.js');
            await checkAvailability();

    # Optional: Discord notification (uncomment and add DISCORD_WEBHOOK secret)
    # - name: Notify Discord
    #   if: success()
    #   uses: Ilshidur/action-discord@master
    #   env:
    #     DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
    #   with:
    #     args: '🏠 VIB Vacation Home availability check completed!'
